[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-m 'not slow and not models'"
markers = [
    "slow: full-pipeline tests with real models (run with: pytest -m slow)",
    "models: real-model contract tests (~90s; run with: pytest -m models)",
]
